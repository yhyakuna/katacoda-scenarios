The vault server has been started in development mode.

First, set the `VAULT_ADDR` environment variable:

```
export VAULT_ADDR=http://localhost:8200
```{{execute}}

Login with root token:

```
vault login root
```{{execute}}
